# Credentials — NEVER commit these
.env
credentials.json
token.json

# Attendee data and certificates — never commit (PII)
input/
output/
*.pdf
*.xlsx

# macOS
.DS_Store

# Python / uv artifacts
__pycache__/
*.py[cod]
.venv/
dist/
*.egg-info/
.pytest_cache/
